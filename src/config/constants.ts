/**
 * 应用常量配置（薄桶）
 * ------------------------------------------------------------------
 * 所有静态配置已解耦至 src/data/static，本文件仅做 re-export。
 * 业务代码仍从 '@/config/constants' 取数，12 个现有消费方 import 路径零改动。
 */

export * from '@/data/static';
