# 2026-07-09 · 设计交付记录

## 今日完成
- 基于 PRD V1.0 完成高考志愿填报 App 7 屏交互原型 Overview
- 视觉风格：阳光橙 #FF7A45 主色 + 金黄 #FAAD14 + 活力绿 #52C41A + 暖白底 #FFF9F5
- 输出文件：`design/prototype-overview.html`
- 7个屏幕：首页、Step1基础信息、Step2选科组合、Step3意向偏好、Step4确认生成、推荐结果页、推荐详情页
- 每屏含设计审查优化 notes，覆盖功能完整性、交互逻辑、信息架构
- 已用 Playwright 截图验证渲染效果

## 关键设计决策
- 选用 Overview 平铺形态（而非 Flow demo），便于逐屏走查
- 四档配色：冲（橙红）→ 稳（金黄）→ 保（草绿）→ 垫（青绿），直觉化风险递减
- 位次反查结果用绿色卡片强提示，强化系统信任感
- 计算依据默认展开，符合"信任型决策顾问"定位
- Step 3 按影响优先级重排：黑名单 > 地域 > 院校 > 权重 > 策略 > 特殊身份

## 待用户确认
- 是否需要在平铺基础上再做一个 Flow demo 可点击版本
- 品牌名"志愿领航"是否沿用或需替换
- 是否有 logo/品牌资产需要替换占位符

---

# 2026-07-09 · 前端代码实现记录（续）

## 前端代码开发完成

### 技术栈
- Vite 5 + React 18 + TypeScript + Tailwind CSS 3 + Zustand 5 + React Router v6 + Framer Motion + Zod + react-hook-form + clsx + lucide-react

### 团队协作流程
- 主理人齐活林协调，架构师高见远完成架构设计，工程师寇豆码（两位agent接力）完成全部代码
- 跳过PRD阶段（已有PRD V1.0），走架构设计→代码实现→QA验证标准SOP
- 架构设计文档：deliverables/architecture/frontend-architecture-2026-07-09.md

### 文件统计
- 总计53个文件（46个src/文件 + 7个根配置文件）
- T01: 项目基础设施+类型+常量（18个文件）
- T02: 状态管理+Hooks+校验引擎（7个文件）
- T03: 公共组件+布局（9个.tsx文件）
- T04: Wizard业务组件+推荐结果组件（12个.tsx文件）
- T05: 7个页面+路由集成（App.tsx + 7个页面）

### 验证结果
- TypeScript类型检查：0错误通过
- Vite生产构建：1974模块编译成功，1.44s完成
- 开发服务器：http://localhost:5173/ 正常运行
- 构建产物：CSS 17.61KB + JS 438.85KB (gzip 138.25KB)

### 关键架构决策
- 不用MUI/Ant Design，选择Tailwind+CSS Variables（高度定制化视觉风格）
- Zustand persist中间件实现localStorage断点续填
- Zod superRefine实现选科非法组合校验
- CSS Variables定义Design Token，Tailwind引用变量名实现单一数据源
- Mock数据策略：前端内置一分一段表模拟+推荐结果Mock

## 2026-07-09 · 数据-逻辑解耦（软件团队 SOP 协作）

### 任务
- 用户要求把写死数据解耦到独立 JSON，区分「静态配置」（运行时不变常量）与「动态数据」（本应后端返回、当前用 JSON mock 充当后端契约样例）

### 协作链路
- 主理人齐活林 → 架构师高见远（数据分类 + JSON 文件树 + 任务分解）→ 工程师寇豆码（落地 12 文件 + 改造 5 文件）→ QA严过关（篡改验证 + 还原）

### 落地产物（10 JSON + 2 加载层 + 改造）
- `src/data/static/`：provinces / subjects / weightTemplates / tierConfigs / options / searchSuggestions / appConfig / subjectCoverage 共 8 个 .json + `index.ts` 加载层
- `src/data/dynamic/`：scoreRank.json + recommendations.json + `index.ts`（含 `getScoreRank` / `getRecommendations` 同步契约函数，注释 TODO 将来替换为后端 fetch）
- `src/config/constants.ts` 改为薄桶 `export * from '@/data/static'`（原 12 个消费方 import 路径零改动）
- `src/utils/subjectRules.ts`、`src/hooks/useScoreRankLookup.ts` 删除内联数据改读 JSON
- `tsconfig.json` 新增 `resolveJsonModule: true`（构建前置，另补了 `@types/node` 依赖）

### 验证结论（QA）
- 篡改 provinces.json(静态) 与 recommendations.json(动态) → 重新构建 → dist 产物均含篡改值 → 证明数据确从 JSON 读取
- 两处已还原，最终 build 通过，遗留问题 0

### 长期约定（本项目）
- 静态配置=运行时不变；动态数据=后端契约 mock；算法/拼装留代码，JSON 只存素材与参数
- 业务代码统一经 `src/data/static|dynamic/index.ts` 取数，禁止直接 import `.json`
