# 2026-07-12 工作日志

## 完成：后端日志系统
- 新建 backend/src/utils/logger.ts（单例 Logger：实时彩色控制台 + 定时3s写盘 + 按天滚动 + 退出/崩溃同步落盘）
- 新建 backend/src/middleware/requestLogger.ts 并注册到 app.ts
- env.ts 增加 LOG_* 配置项；index/error/recommendation/llm/db.pool/mail/scoreRank 全部接入 logger 替换 console.*
- backend/src/types/nodemailer.d.ts 兜底类型（消除 tsc 的 TS7016）
- 验证：tsc 通过；实跑确认控制台彩色输出 + logs/app-2026-07-12.log 生成且含 HTTP 请求行

## 完成：前端动效升级
- 安装 canvas-confetti、@types/canvas-confetti、sonner
- 新建 PageTransition / AppToaster / utils/confetti.ts / hooks/useCountUp.ts
- 增强 AppLayout(包转场) / RecCard(悬浮+点击) / ResultsPage(彩带+Toast+stagger) / BottomCTA(流光shimmer) / TierTabs(layoutId滑动指示) / HitRateRing(SVG描边+count-up) / HomePage(stagger+浮动) / LoadingOverlay(呼吸光晕) / main.tsx(挂Toaster)
- 修复 profile.ts 预存 TS 错误使 npm run build 通过；前端 build 通过(1995模块)，vite dev 正常

## 完成：腾讯云部署方案（首席技术支持官 + CloudQ）
- 建立团队 cloud-ops-deploy-gaokao，调度 CloudQ 出《腾讯云部署架构方案》全文（产品选型/拓扑/成本TCO/步骤/风险/本项目注意点）
- 已生成可直接上云部署文件：
  - backend/Dockerfile（多阶段 Node20，含 /health 健康检查，tsc 验证零报错）
  - backend/.dockerignore（排除 node_modules/.env/日志）
  - deploy/nginx.conf（托管前端 dist + 反代 /api → api:8080）
  - deploy/docker-compose.prod.yml（api + web 编排，数据库用 TencentDB，含可选本地 PG 注释）
  - deploy/.env.prod.example、deploy/DEPLOY.md、deploy/ci-github-actions.example.yml
  - deliverables/deployment/tencent-cloud-deploy-guide.md（主交付文档，整合 CloudQ 方案 + 部署文件）
- 关键结论：推荐架构=前端COS+CDN｜后端CVM-S5+Docker｜TencentDB PG高可用16｜DeepSeek直连+SSM密钥托管；成本最低≈¥352/月，推荐≈¥1.8k–3.3k/月
- 关键发现：本项目前端用相对路径 /api/v1（非 import.meta.env），故走 Nginx 反代可零改前端代码上线（Path B 快速路径）；CloudQ 默认 COS+CDN 路径（Path A）需前端加运行时 config.js
- 验证：backend 本地 npm ci + tsc 编译零报错；前端 dist/ 已存在
- 待确认：主地域、Path A/B 选择、CloudQ 凭证（env 检测 exit code 2 未配置）、等保排期

## 决策锁定（用户确认）：上海 + Path B
- 用户确认：地域=上海(ap-shanghai)，路径=Path B（单台 CVM+Docker，Nginx 反代，零改前端）
- 新增 deploy/SHANGHAI-PATHB-RUNBOOK.md（上海专线执行手册：购买清单/VPC/DB/上传/启动/HTTPS/上线清单）
- 更新 deploy/.env.prod.example：DATABASE_URL 改为上海内网示例(sh-xxxxxx.pg.ap-shanghai.tencentcloud.com) + TENCENT_REGION=ap-shanghai
- 更新主指南第十二节：标记已确认决策并指向 runbook
- 当前状态：部署包已可在拿到 CVM+TencentDB 后 3 命令上线；仍缺真实云凭证无法实际开通资源

## 决策：Railway 免费后端版本
- 用户选 Railway 替代 Render（不休眠、$5/月额度制、冷启动快，比 Render 更适合真实早期用户）
- 新增 deploy/railway.json（rootDirectory=backend, nixpacks, build=npm ci && npm run build, start=npm run start, healthcheck=/health, 失败重启）
- 新增 deploy/RAILWAY-DEPLOY.md（完整 Railway 部署指南：Supabase建库+迁移 / Railway / Vercel rewrite / 验证 / 升级回腾讯云）
- 更新 deploy/FREE-DEPLOY.md：把 Railway 从"备选"提升为"推荐后端"，加引用指向 RAILWAY-DEPLOY.md
- 排雷确认：后端 env.ts 已 `parseInt(process.env.PORT ?? '8080')`，Railway/Render/腾讯云均兼容，零改代码
- 免费组合（推荐）：Vercel(前端免费) + Railway(后端$5额度不休眠) + Supabase(免费PG 500MB)

## 决策：免费部署云端（零成本替代路径）
- 用户要求"免费部署云端"：腾讯云 CVM+TencentDB 无长期免费额度，改用免费 PaaS 组合
- 方案：前端 Vercel（纯静态 + rewrite 代理 /api）+ 后端 Render free（Node runtime 512MB 会休眠）+ 数据库 Supabase free PostgreSQL（500MB）
- 已生成：vercel.json（根目录，/api/:path* → Render 后端，前端零改）、deploy/render.yaml（rootDir=backend, build=npm ci && npm run build, start=node dist/index.js, healthCheck=/health, sync:false 变量）、deploy/.env.free.example、deploy/FREE-DEPLOY.md（Supabase建库+迁移/Render/Vercel/验证/升级回腾讯云）
- 限制（已诚实告知）：Render free 15分钟休眠+冷启动30s+；Supabase 500MB；免费层扛不住高考季洪峰，定位 MVP 验证/演示；正式上线回上海 Path B
- 关键：前端仍相对路径 /api/v1，Vercel rewrite 代理到 Render，浏览器同源无需 CORS，业务代码零改动

## Railway 已注册 → 消除两个部署阻塞点
- 用户确认 Railway 账号已注册（免费路线后端用 Railway）
- 本地 macOS 无 pg_dump/psql，且项目 SQL 脚本(01~04)是自包含 DDL+种子INSERT；故生成 deploy/supabase-init.sql（合并 01建表/02索引/03种子/04认证），用户粘到 Supabase SQL Editor 一次执行即建库+灌种子，绕开 pg_dump 依赖
- 把 deploy/railway.json 复制到仓库根目录 railway.json，Railway 从 GitHub 导入即自动识别（rootDir=backend, nixpacks, /health 健康检查），少配一步
- 剩余阻塞：Supabase/Vercel 待注册；vercel.json rewrite 目标待填真实 Railway 域名（xxx.up.railway.app）；DEEPSEEK_API_KEY 待填 Railway Variables
- 免费组合最终定稿：Vercel(前端) + Railway(后端$5额度不休眠) + Supabase(免费PG 500MB)

## 三平台已注册 → 我本地能做的全部做完
- 用户确认 Supabase/Railway/Vercel 均已注册
- 创建 .gitignore（原本不存在！根目录 .env 有密钥风险）：忽略 .env/.env.*/backend/.env/node_modules/dist/logs/*.tsbuildinfo；保留 .env.*.example
- 检查：根 .env 未被 git 跟踪（未泄露），建好 .gitignore 后确保不会误提交
- 修正 vercel.json：rewrite 目标从 Render 默认域名改为 Railway 占位 https://REPLACE-WITH-RAILWAY-DOMAIN.up.railway.app/api/:path*（待用户拿到 Railway 域名替换）
- 生成 deploy/FREE-DEPLOY-CHECKLIST.md：三控制台精确到按钮的执行清单（Supabase建库+验证查询 / Railway部署+变量+Usage$5+拿域名 / Vercel改1行+部署），并标注我无法代劳（需登录控制台+用户私有凭证）
- 我无法代劳的部分已清单化；部署动作本身需用户控制台操作（无 token/需用户会话）

## 免费部署收尾改进（19:17）
- 去除"等用户发 Railway 域名我再改 vercel.json"的卡点：vercel.json rewrite 改为引用 Vercel 环境变量 `https://${RAILWAY_BACKEND_URL}/api/:path*`，用户在 Vercel Settings→Environment Variables 填 RAILWAY_BACKEND_URL=项目名.up.railway.app（不含https://）即可，无需改文件、无需回传域名
- 同步更新 FREE-DEPLOY-CHECKLIST.md ③步与自检项
- 校验：vercel.json/railway.json 均为合法 JSON；supabase-init.sql 372 行已就位；根 railway.json 398 字节
- 当前完整部署包：Vercel(前端) + Railway(后端$5额度) + Supabase(免费PG) 三平台配置齐备，本地可做的全部做完，剩余仅为用户控制台点击+填私有凭证
